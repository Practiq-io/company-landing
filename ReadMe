внутри приложение для разработки в Реакт (create-react-app) с первоначальной блочной структурой сайта.

после скачивания проекта, требуется установить зависимости (Node_Module), для этого в терминале vscode пишим команду: -npm i //файлов много, качает долго, 250мб где-то
после установки зависимостей, пишим команду в терминале vscode: -npm start, после чего на локальном порте 3000 откроется новое окно в браузере, после чего можно приступать к верстке.
